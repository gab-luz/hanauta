"""Settings pages extracted from the legacy monolith.

Each module should expose small builder functions that take the Settings window
instance and return widgets for composition in `settings.py`.
"""

